#include "main.h"
#include <unistd.h>
#include <stdio.h>

/**
 *_putchar - putchar
 * @c : char to use
 * description : print char
 * Returns : char
 */
int _putchar(char c)
{
return (write(1, &c, 1));
}
